require 'rake'

task :default => :test

namespace :compile do
  desc "Compiling everything"
  task :all do
    
  end
  
  desc "Compile the SMILKit library as a SWC and SWF"
  task :lib do
    
  end
  
  desc "Build documentation"
  task :docs do
    
  end
end

desc "Package SMILKit builds into a .zip for distrubtion"
task :package do
  
end

desc "Run FlexUnit Tests"
task :test do
  # compile + launch?
end